server:
  port: 8082
spring:
  liquibase:
    enabled: true
    change-log: classpath:db.changelog/db.changelog-master.yml
  shell:
    interactive:
      enabled: false
  datasource:
    url: jdbc:postgresql://localhost:5432/libraryTest
    username: postgres
    password: 1240562Na
    driver-class-name: org.postgresql.Driver
    jpa:
      database: postresql
      database-platform: org.hibernate.dialect.PostreSQL10Dialect
